# Commit
Run `git status` and `git diff --staged`. Confirm node_modules/dist are gitignored. Show me the files before staging. Only stage files I approve, then commit with a detailed message. Never push or open a PR unless I ask.

For the commit message:
- Use a short subject line (feat/fix/refactor/chore: summary, max ~72 chars)
- Add a body with bullet points explaining the key changes — what was added, removed, or restructured, and why where relevant
- Be specific: name the components, functions, or modules affected

Never mention that it is authored by claude
