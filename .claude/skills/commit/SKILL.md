# Commit
Run `git status` and `git diff --staged`. Confirm node_modules/dist are gitignored. Show me the files before staging. Only stage files I approve, then commit with a concise message. Never push or open a PR unless I ask.

never mention that it is authored by claude
